% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rZ_bipartite.R
\name{rtnorm_interval_logp}
\alias{rtnorm_interval_logp}
\title{Exact truncated-normal draw on (lb, ub) via the log.p-scale inverse CDF}
\usage{
rtnorm_interval_logp(ez, lb, ub)
}
\arguments{
\item{ez}{vector of conditional means.}

\item{lb}{lower bounds (recycled to \code{length(ez)}).}

\item{ub}{upper bounds (recycled to \code{length(ez)}).}
}
\value{
vector of truncated-normal draws, same length as \code{ez}.
}
\description{
Draws \code{z ~ N(ez, 1)} truncated to \code{(lb, ub)} for each cell,
working entirely on the log-probability scale in the tail that
contains the truncation interval. This stays exact for arbitrarily
extreme \code{ez} / bounds (log-scale \code{pnorm}/\code{qnorm} are
accurate to \code{|ez|} of several hundred), so no probability clamp
is needed. Infinite bounds reduce to the
one-sided case automatically.
}
\author{
Cassy Dorff, Shahryar Minhas, Tosin Salau
}
\keyword{internal}
