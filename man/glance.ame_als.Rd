% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_lame.R
\name{glance.ame_als}
\alias{glance.ame_als}
\alias{glance.lame_als}
\title{Glance method for fitted \code{ame_als} / \code{lame_als} objects}
\usage{
\method{glance}{ame_als}(x, ...)

\method{glance}{lame_als}(x, ...)
}
\arguments{
\item{x}{A fitted \code{ame_als} / \code{lame_als} object.}

\item{...}{Ignored.}
}
\value{
One-row data frame with columns \code{nobs},
\code{n_actors}, \code{n_periods}, \code{family}, \code{mode},
\code{R}, \code{converged}, \code{iterations}, \code{se_source}.
}
\description{
One-row data frame summarising an ALS fit, compatible with
\pkg{broom} / \pkg{modelsummary}. Reports observation count, actor
count, latent dimension, family, mode, ALS convergence flag, and
iteration count.
}
\examples{
\donttest{
data(YX_bin_list)
Y1 <- 1 * (YX_bin_list$Y[[1]] > 0); diag(Y1) <- NA
fit <- ame_als(Y = Y1, Xdyad = YX_bin_list$X[[1]],
               family = "binary", R = 1, verbose = FALSE)
glance(fit)
}
}
