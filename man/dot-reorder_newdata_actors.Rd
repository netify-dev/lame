% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linear_predictor.R
\name{.reorder_newdata_actors}
\alias{.reorder_newdata_actors}
\title{Realign a newdata covariate array to the fit's internal actor order}
\usage{
.reorder_newdata_actors(X, row_order, col_order)
}
\arguments{
\item{X}{an \code{n x m x p} array (already promoted from a matrix).}

\item{row_order, col_order}{canonical actor names from
\code{\link{.fit_actor_order}}.}
}
\value{
\code{X} with rows / columns permuted into canonical order, or
unchanged.
}
\description{
Reorders the rows and columns of each \code{n x m x p} slice by their
dimnames so they line up with the fit's stored (sorted) additive /
multiplicative effects. Only acts when \code{newdata} carries dimnames whose
name set exactly matches the fit's; otherwise it falls back to the supplied
positional order unchanged (the historical behaviour, correct when the user
already passes actors in the fit's order or supplies no names).
}
\keyword{internal}
