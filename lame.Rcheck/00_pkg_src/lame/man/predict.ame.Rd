% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.ame.R
\name{predict.ame}
\alias{predict.ame}
\title{Predict method for AME models}
\usage{
\method{predict}{ame}(
  object,
  newdata = NULL,
  type = c("response", "link", "distribution"),
  n_samples = 100,
  include_uncertainty = TRUE,
  ...
)
}
\arguments{
\item{object}{Fitted AME model object}

\item{newdata}{Optional dyadic covariates for prediction: a single
3-D array (\code{n x n x p_dyad}) whose slices match the dyadic
covariates the model was fit with (same order; do not include the
intercept or nodal-covariate slices). Nodal covariates cannot be
changed at prediction time -- their fitted contribution is reused.
When omitted, the fitted design is used.}

\item{type}{Character; type of prediction:
\itemize{
\item "response": predicted values on response scale (default)
\item "link": predicted values on link scale
\item "distribution": full posterior predictive distribution
}}

\item{n_samples}{For type="distribution", number of posterior samples}

\item{include_uncertainty}{Logical; include parameter uncertainty (default TRUE)}

\item{...}{Additional arguments (not used)}
}
\value{
Depending on type:
\itemize{
\item "response"/"link": Matrix of predictions
\item "distribution": Array of posterior predictive samples
}
}
\description{
Generate predictions from fitted AME models, including point estimates
and predictive distributions.
}
\examples{
\donttest{
# Fit model
data(YX_nrm)
fit <- ame(YX_nrm$Y, Xdyad = YX_nrm$X, R = 2,
           nscan = 100, burn = 10, odens = 1, verbose = FALSE)

# Point predictions
Y_pred <- predict(fit)

# Predictions on link scale
Y_link <- predict(fit, type = "link")
}

}
\author{
Cassy Dorff, Shahryar Minhas, Tosin Salau
}
