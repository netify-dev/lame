% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{sample_beta_dynamic_cpp}
\alias{sample_beta_dynamic_cpp}
\title{Sample the dynamic-block beta path via FFBS}
\usage{
sample_beta_dynamic_cpp(
  Xdyn_list,
  Xstat_list,
  Z_list,
  offset_list,
  beta_static,
  rho_by_coef,
  sigma_by_coef,
  Lambda,
  beta0_mean,
  beta0_cov,
  s2,
  dyad_rho,
  bipartite,
  symmetric,
  use_dyad_rho
)
}
\arguments{
\item{Xdyn_list}{T-length list of (n*n) x p_dyn long-format design
matrices for the dynamic block (column-major reshape per period).}

\item{Xstat_list}{T-length list of (n*n) x p_static long-format design
matrices for the static block.}

\item{Z_list}{T-length list of (n x n) latent matrices.}

\item{offset_list}{T-length list of (n x n) offset matrices (a_i + b_j +
U_i'V_j contributions; everything that's not in X*beta).}

\item{beta_static}{Length p_static current static beta vector.}

\item{rho_by_coef}{Length p_dyn vector of AR(1) rho values for each
dynamic coefficient. (Per-block but expanded per-column for vectorised
indexing.)}

\item{sigma_by_coef}{Length p_dyn vector of AR(1) innovation standard
deviations for each dynamic coefficient.}

\item{Lambda}{Block-diagonal innovation scale matrix (p_dyn x p_dyn).
Combined with sigma^2 to give Q = sigma^2 * Lambda.}

\item{beta0_mean}{Length p_dyn prior mean for the first state beta_1
(the prior is placed directly on beta_1, no predict step at t = 1).}

\item{beta0_cov}{p_dyn x p_dyn prior covariance for beta_1. Must be a
fixed matrix that does not depend on the current (rho, sigma) draw so
the transition-only hyperparameter conditionals remain exact.}

\item{s2}{Dyadic variance.}

\item{dyad_rho}{Dyadic correlation (ignored when use_dyad_rho=FALSE).}

\item{bipartite}{Whether the network is bipartite.}

\item{symmetric}{Whether the network is symmetric.}

\item{use_dyad_rho}{Whether to use the dyad-corr branch (TRUE only for
unipartite, asymmetric, with a non-zero rho).}
}
\value{
List with: path -- a (T x p_dyn) matrix of beta draws (one row
per period); chol_fail -- integer count of Cholesky failures.
}
\description{
Forward-filter / backward-sample the AR(1) state-space model for the
dynamic-block beta coefficients. Returns the joint draw of beta_dyn at
every time period.
}
