% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cowles_cutpoints.R
\name{sample_alpha_cowles}
\alias{sample_alpha_cowles}
\title{Cowles MH update for explicit ordinal cutpoints (Z-marginalised)}
\usage{
sample_alpha_cowles(alpha, Y_int, EZ, tau_prop, symmetric = FALSE)
}
\arguments{
\item{alpha}{numeric, current cutpoints with \code{alpha[1] = 0},
length \code{K-1}.}

\item{Y_int}{integer matrix (or array) of ordinal categories coded
1..K, with \code{NA} for missing cells.}

\item{EZ}{linear predictor at the dyad level, same shape as
\code{Y_int}.}

\item{tau_prop}{numeric, RW proposal SD in delta space (will be
adapted in burn-in by the caller).}

\item{symmetric}{logical, whether the network is symmetric (restricts
the likelihood to upper-triangle cells so each dyad is counted once;
variance 1, no precision rescaling).}
}
\value{
list with elements \code{alpha}, \code{delta}, \code{accept}
(logical).
}
\description{
One Metropolis-Hastings sweep on \code{alpha} with Z integrated out
of the conditional. The proposal is a symmetric random walk in
\code{delta = log(diff(alpha))} space; the acceptance ratio uses
the marginal ordinal-probit likelihood. Mixing of \code{alpha}
under this update is dramatically faster than the data-induced
Gibbs convention (Cowles 1996 reports ESS gains of an order of
magnitude on standard ordinal-probit benchmarks).
}
\author{
Cassy Dorff, Shahryar Minhas, Tosin Salau
}
\keyword{internal}
