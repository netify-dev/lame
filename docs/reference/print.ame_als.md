# Print an ame_als object

Print an ame_als object

## Usage

``` r
# S3 method for class 'ame_als'
print(x, digits = 4, ...)
```

## Arguments

- x:

  an `ame_als` object.

- digits:

  number of digits to display.

- ...:

  ignored.

## Value

`x`, invisibly.
