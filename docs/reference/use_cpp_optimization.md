# Use optimized C++ functions when available

This file contains wrapper functions that automatically use optimized
C++ implementations when available, falling back to R implementations if
needed.

## Usage

``` r
use_cpp_optimization()
```
